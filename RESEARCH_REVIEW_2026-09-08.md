# 논문 프로젝트 전체 검토 — 2026-09-08

## 검토 범위와 판단

연구 설계, P01~P03 초기 반복·대응 실험, P04~P06 경보 실험, P04 정책 비교, 9~10단계 교체, 후속 생성·지표·AI 평가·사람 예비 평가 및 분석 코드를 검토했다. 새로운 이미지 생성이나 사람 평가를 수행하지 않았다. 이번 검토에서 구글 슬라이드의 최신 화면과 모든 생성 이미지를 다시 육안 평가하지는 않았다.

현재 결과는 **비얼굴 순차 편집에서 얼굴 변화와 원본 기반 재생성의 이득·한계를 다룬 탐색적 실증 연구**를 뒷받침한다. 일반적인 자동 열화 감지기, 최적 개입 정책, 새로운 복원 알고리즘의 성능 검증은 아직 아니다. 현재 RESEARCH_DESIGN.md가 이 범위를 명시한 것은 타당하다.

## 확보한 근거

- 생성 인물 원본 6개, 순차 경로 7개. P03 두 경로는 같은 인물의 반복이다.
- 후속 분석은 정책 경로 20개·관측 단계 198개·지표용 고유 출력 113개·AI 얼굴 평가 고유 출력 111개를 구분한다. 공유 출력과 대조·반복을 독립 표본으로 중복 계산하지 않는다.
- 현행 최종 고유 이미지 16개에 대한 R01의 예비 평가는 순차 최종 7개를 모두 심함·미유지, 원본 기반 최종 6개를 모두 없음 또는 약함·유지로 기록했다.
- 두 AI 세션의 이진 등급 일치 107/111은 같은 기반 모델의 세션 간 일치다. 사람 평가자 간 신뢰도나 정답률이 아니다.
- 실패한 P05 단일 개입 경로는 8단계까지만 관측됐으며, 없는 최종 결과를 채우지 않았다.
- 최초 출력, 실제 프롬프트, 입력 연결, 해시, 실패·공유 기록, 정합 민감도 분석을 보존했다. 이는 결과 추적 가능성의 강점이다.

근거: [연구 설계](RESEARCH_DESIGN.md), [종합 결과](experiments/nonhuman-followup-v1/RESULTS.md), [재현 절차](experiments/nonhuman-followup-v1/REPRODUCE.md).

## 우선 보완할 사항

### 1. 기여를 관찰 결과와 검증되지 않은 방법 제안으로 구분

반복 편집의 누적 오차와 원본 참조는 이미 선행 연구가 다루는 주제다. 새로움은 단순히 원본에서 다시 생성한다는 아이디어보다, 비편집 얼굴에서 지표·지각 판단의 차이와 재생성 이후 지속성·호출 비용을 함께 보여주는 데 두는 것이 방어 가능하다. 이는 현재 자료에 대한 검토 판단이지 신규성의 확정 판정은 아니다.

현재 주장할 중심 결과는 (a) 주어진 조건에서의 최종 얼굴 보존 차이, (b) 원본 차이 점수와 지각적 열화의 불일치, (c) 중간 재생성 이후 재발 사례다. 자동 감지·최적 시점을 핵심 기여로 삼으려면 별도 개발/검증 자료와 단계별 사람 평가가 필요하다.

이번 확인 문헌: [Multi-turn Consistent Image Editing](https://arxiv.org/abs/2505.04320), [EdiVal-Agent](https://arxiv.org/abs/2509.13399). 기존 상세 검토는 [SOURCE_REVIEW.md](experiments/nonhuman-followup-v1/literature/SOURCE_REVIEW.md)에 있다.

### 2. 평가자 수와 생성 실험 수는 별개의 보완 과제

공개 설문에 더 많은 사람이 참여하면 같은 16개 이미지에 대한 판단의 안정성을 평가할 수 있다. 그러나 인물·출력 표본은 늘지 않는다. 대부분의 인물·방법은 생성 1회이고, 정책 전체 비교는 P04 한 인물에 집중돼 있다. 다른 인물·편집 경로에도 효과가 유지된다는 주장을 강화하려면 신규 원본과 독립 생성 반복이 필요하다.

평가자 수 × 16개 이미지를 모두 독립 관측으로 취급하면 안 된다. 후속 분석은 동일 평가자의 반복 응답, 동일 원본의 여러 출력, P03의 공유 재생성 결과를 반영해야 한다. 본격 수집 전에 주 평가항목, 판단 어려움 처리, 제외 규칙, 모집 종료 기준, 효과 크기·불확실성 산출 방법을 정해야 한다. 임의의 모집 인원만으로 검정력이 확보됐다고 주장하지 않는다.

### 3. 단계 수와 편집 내용의 효과가 분리되지 않음

모든 경로가 유사한 고정 순서를 따르므로 뒤 단계일수록 호출 횟수뿐 아니라 요구 내용·복잡도도 달라진다. 현재 결과는 이 경로에서 열화가 관측됐다는 근거이며, 반복 횟수만이 열화 원인이라는 증거는 아니다.

이를 구분하려면 같은 최종 요구에 도달하는 다른 길이·순서의 경로나 무변경 반복 대조를 별도 실험으로 둘 수 있다. 모든 대조를 즉시 추가할 필요는 없으며, 논문에서 주장하려는 원인에 맞춰 선택해야 한다.

### 4. 사후 설계 변경과 반례를 본문에 유지

9~10단계는 포켓스퀘어 요구 오류를 관찰한 뒤 셔츠 색·배경 변경으로 교체했다. 기록은 정직하게 남아 있지만, 현행 실험을 처음부터 고정된 독립 확증 실험처럼 설명하면 안 된다. 실패 요구와 변경 이유를 방법·한계에 명시해야 한다.

2026-09-08 정리: 형식이 다른 과거 후보 선택·사용 가능 응답과 이를 인용한 대조는 프로젝트에서 삭제했다. 결과의 유불리와 무관하게 프로토콜 유형을 기준으로 제외했으며, 삭제한 평가를 결론의 근거로 사용하지 않는다. 단계별 0~3 얼굴 열화 평가와 설계 변경 이력은 유지한다.

근거: [평가 정리 기록](EVALUATION_CLEANUP.md), [단계 교체](experiments/revised-tail-v1/PROTOCOL.md).

### 5. 정책 효율성은 아직 제한된 사례 비교

P04의 매 단계 원본 방식 10호출과 경보마다 재생성 18호출은 유효한 논리 비용 비교다. 그러나 3~10단계 결과는 같은 저장 출력을 공유한다. 독립 생성으로 품질 동등성이나 일반적 비용 우월성이 검증된 것은 아니다. 돈·시간 절약률로 바꾸어 표현해서도 안 된다.

자동 개입의 가치를 주장하려면 새 인물에서 매 단계 원본 방식과 비교하고, 고정 개입과 감지 개입에는 같은 호출 예산을 부여해야 한다. 원본 기반 재생성이 이전 출력의 말로 기록되지 않은 세부 표현까지 유지하는지도 현재 실험은 확인하지 않았다.

### 6. 높은 AUC를 감지 정확도로 해석하지 않기

고정 SSIM AUC 0.952는 R01이 평가한 최종 16개에서의 기술 통계다. 양성·음성이 정책과 강하게 연결돼 있으므로, 같은 정책 안의 정상/열화 구분이나 새로운 인물의 최초 열화 시점 감지 성능을 입증하지 않는다. 평가자 확대만으로 이 제한이 사라지지는 않는다.

얼굴 요구 충족과 얼굴 보존도 분리해야 한다. 현행 최종 편집 요구는 AI 두 세션이 평가했고, 사람은 얼굴 두 항목만 평가했다. 따라서 최종 편집 요구가 사람에게도 검증됐다고 쓰지 않는다. 이전 사람 평가의 요구 충족을 새 최종 이미지에 대입하지 않는다.

### 7. 새 공개 설문에서 논문 분석으로 이어지는 처리가 남음

설문 수집 기능과 별개로, `analyze_human_final.py`는 기존 `current-final-human-v1-face-*` 및 `current-final-human-v2-requirements-*` JSON만 읽는다. 새 설문의 `public-final-human-v2` 제출이나 CSV를 읽는 연결은 현재 코드에서 찾지 못했다.

새 형식의 수입·검증, 후보 ID/이미지 해시 대조, 평가자별 두 문항 연결, 예비 R01과 공개 모집 집단 구분이 필요하다. 기존 코드는 일부 후보 매핑에서 평가자 구분 없이 한 행을 남기므로 다수 평가자용 분석으로 그대로 확장하지 않아야 한다. 이는 현재 R01 결과의 오류라는 뜻은 아니다.

근거: [사람 분석 코드](experiments/nonhuman-followup-v1/analyze_human_final.py), [공개 설문 정의](evaluation/lib/survey.ts).

### 8. 논문 본문과 재현 패키지 정리

현재 저장소에서 연구 설계·결과 보고서는 확인했지만 완성된 논문 본문 파일은 찾지 못했다. 논문 작성 단계에서는 예비 관찰→대응 비교→정책·지속성→현행 최종 평가 순서로 연결하고, 핵심 주장마다 사용한 자료 버전을 명시해야 한다.

모델 내부 버전·시드가 노출되지 않으므로 픽셀 단위 생성 재현은 보장할 수 없다. 대신 저장 이미지 기반 분석 재현을 제공해야 한다. 현재 RESEARCH_INDEX는 공개 저장소가 전체 실행 패키지가 아님을 명시하고 있다. 외부 재현을 목표로 한다면 실행 의존성·상대 경로·필수 자료·버전 고정을 별도로 정리해야 한다.

## 권장 진행 순서

1. 현재 좁힌 연구 질문을 유지하고 핵심 주장·주 평가항목을 확정한다.
2. 공개 응답을 읽는 별도 분석과 다수 평가자 처리 규칙을 먼저 준비한다.
3. 사람 평가로 기존 출력에 대한 판단의 안정성을 확인한다.
4. 신규 인물·생성 반복·다른 요구 경로로 방향을 재검증한다. 자동 개입 주장을 유지하는 경우에만 개발/검증 분리와 같은 예산 정책 대조를 추가한다.
5. 초기 반례와 사후 변경까지 포함해 논문 본문과 재현 자료를 구성한다.

## 이번 실행 검증

저장된 자료에 대한 기존 검증 테스트 47개(사람 분석 4, AI 평가 16, 확장 지표 16, 지표 무결성 11)가 모두 통과했다. 이 검사는 출처·해시·집계·지표 계산의 일관성 검사이며 연구의 일반화나 통계적 검정력을 보증하지 않는다.

추가로 `join_reviews.py`의 쓰기를 메모리 캡처로 바꿔 재계산한 4개 결과가 저장본과 모두 일치했다. 사람 분석도 임시 폴더에 재계산해 모든 산출물이 저장본과 바이트 단위로 일치함을 확인했다. 원본 평가·이미지·기존 결과 파일은 수정하지 않았다.
