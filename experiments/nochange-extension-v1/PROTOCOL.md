# 변경 없는 생성 3→6단계 연장 — 사전 계획

2026-09-09. 기존 edit-decomposition-v1의 P04 no_change 두 경로 1~3단계 결과를 본 뒤, 두 경로 모두 4~6단계로 연장한다. 새 호출은6회이며 새로운 독립 경로가 아니다. 초기 증가를 보고 설계한 조건부 후속 관찰이며 신규6단계 반복 연구로 부르지 않는다.

각 직전 출력을 입력으로 같은 변경 없는 프롬프트를 그대로 사용한다. 매 단계 r1/r2의 독립 두 호출만 병행할 수 있다. 6회 상한, 첫 출력 보존, 재시도 없음. 실제 생성 seed·모델 버전은 노출되지 않는다.

기존 P04 얼굴 ROI와 이마·양볼 좌표를 그대로 사용한다. 원본 대비와 직전 대비 MAE/SSIM/LPIPS, 피부 MAE/SSIM/고주파 MAE를 고정 ROI 및 NCCsigma1/3/6으로 측정한다. 평균색/잔차도 분석한다. 초기 단계의 자료는 출처를 표시해 연결한다. 원본 거리의 증가, 직전 거리의 변화 및 두 경로 차이를 기술하며 단조성·포화·선형 누적을 전제하지 않는다. 길이6까지 관측이 포화의 증거라고 단정하지 않는다.

사람 평가·독립 AI 등급·열화 정답을 만들지 않는다. 변경 없는 요청도 수치상 원본에서 달라질 수 있으며 자연스러움이나 피부 품질 악화와 동일시하지 않는다.
