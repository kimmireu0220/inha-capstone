# 인물 이미지 순차 편집의 얼굴 열화와 원본 기반 재생성

최신 전체 범위는 [현재 상태](CURRENT_STATUS.md)를 기준으로 합니다. 아래 기본 설계·16개 평가 수치는 초기10단계 실험을 설명하며, 후속 확장은 마지막 절에 구분합니다.

## 연구 질문

비얼굴 요소를 반복해서 수정할 때 요청하지 않은 얼굴 열화가 발생하는가? 얼굴 열화가 발생한 경우 최초 원본에 현재까지의 요구사항을 일괄 적용하는 재생성이 순차 편집보다 얼굴을 더 잘 보존하는가?

## 비교 방법

- 순차 편집: 직전 단계의 결과에 다음 요구를 추가한다.
- 원본 기반 재생성: 최초 인물 원본에 해당 시점까지의 현재 요구사항을 한 번에 적용한다.
- 변경된 조건은 과거 표현을 누적하지 않고 최신 상태로 대체한다.
- 각 호출의 첫 성공 결과만 사용하며 품질을 보고 재시도하거나 최선 결과를 선택하지 않는다.
- 초기 조건은 얼굴을 직접 바꾸지 않는 장신구·의상·배경 편집10단계다. 후속 실험은 별도 계획으로 실행했다.

원본은 6명이고 순차 경로는 7개다. P03의 두 경로는 같은 원본의 반복이므로 별도 인물로 세지 않는다. 정책 간 동일한 입력·프롬프트·출력은 공유 자료로 기록한다.

## 평가 축

### 연속 지표

동일한 얼굴 영역에서 MAE, SSIM, LPIPS를 계산한다. 고정 영역과 정수 평행이동을 보정한 영역을 함께 제시한다. 점수는 원본과의 차이이며 피부 열화 자체의 정답은 아니다.

현 단계에서는 특정 임계값을 정하지 않는다. 연속 점수와 AI·사람 등급의 관계를 Spearman 순위 상관과 ROC-AUC로 비교하고, 동일 인물의 순차 편집·일괄 재생성 쌍에서 점수 방향이 일치하는지 확인한다.

### 독립 AI 보조 평가

방법명·단계·지표를 가린 얼굴 영역을 두 개의 독립 세션이 평가한다. 피부의 부자연스러운 무늬와 변형을 0~3으로 기록한다. 두 세션은 같은 기반 모델이므로 서로 다른 모델의 합의나 사람 정답으로 해석하지 않는다.

### 사람 예비 평가

최종 고유 이미지 16개에 대해 R01 한 명이 두 항목을 평가했다.

- 얼굴 열화: 없음·약함·뚜렷함·심함
- 얼굴 보존: 유지·일부 유지·미유지

편집 요소는 독립 AI 평가에서 모든 후보가 충족된 것으로 관찰되어 반복적인 사람 문항에서 제외했다. 사람 1명의 결과는 예비 기술 통계로만 사용한다.

## 현재 결과

- 독립 AI 두 세션의 단계별 고유 출력 111개에 대한 뚜렷한 열화 이진 판단은 107/111(96.40%) 일치했다.
- 사람 R01은 순차 편집 최종 7개를 모두 `심함·미유지`, 종료 시 원본 기반 재생성 최종 6개를 모두 `없음 또는 약함·유지`로 평가했다.
- 과거 임시 규칙이 반응한 시점에 한 번만 재생성하고 편집을 계속한 최종 2개는 모두 `심함·미유지`였다. 한 번의 중간 재생성은 이후 열화 재발을 막지 못했다.
- 사람의 뚜렷한 열화 판정과 AI 두 세션의 이진 판단은 각각 15/16(93.75%) 일치했다.
- 사람 최종 16개의 0~3 등급과 연속 지표의 Spearman rho는 고정 MAE 0.492, 1−SSIM 0.717, LPIPS 0.661이었다. 위치 보정값은 각각 0.494, 0.734, 0.668이었다. 보조 ROC-AUC는 고정 영역에서 각각 0.794, 0.952, 0.937이었다.
- 동일 인물·반복 7쌍에서 위치 보정 MAE·SSIM·LPIPS는 모두 7/7 순차 편집의 원본 차이가 더 컸다.

## 임계값 기반 과거 실험의 위치

초기 P01의 2·3단계 사이에서 정한 임시 임계값은 일부 개입 경로를 생성하기 위한 탐색 규칙이었다. 표준 기준이나 검증된 감지기가 아니므로 현행 핵심 분석에서 제외한다. 해당 경로와 결과는 연구 이력으로 보존하되 임계값의 정확도·민감도·특이도나 자동 개입 성능을 주장하지 않는다.

자동 개입 시스템까지 확장하려면 새로운 인물을 개발군과 검증군으로 분리해야 한다. 개발군의 단계별 사람 라벨로 지표와 임계값을 선택한 뒤, 검증군에서는 이를 변경하지 않고 평가해야 한다.

## 현재 주장 범위

현재 자료는 이 편집 조건에서 다음을 지지한다.

1. 반복적인 비얼굴 편집은 최종 얼굴의 인위적 변화와 얼굴 보존 실패를 동반했다.
2. 최종 시점의 원본 기반 일괄 재생성은 순차 편집보다 얼굴 차이와 사람이 본 열화를 줄였다.
3. 중간에 한 번 재생성한 뒤 다시 순차 편집하면 열화가 재발할 수 있다.
4. MAE·SSIM·LPIPS는 유용한 변화 신호지만 피부 열화 전용 감지기는 아니며, 현재 자료만으로 하나의 임계값을 정할 수 없다.

다른 생성 모델·실제 인물 사진·복잡한 얼굴 편집으로의 일반화, 최적 재생성 시점, 실시간 자동 감지 정확도, 완전한 얼굴 복원은 주장하지 않는다.

## 주요 자료

- 통합 결과: `experiments/nonhuman-followup-v1/RESULTS.md`
- 사람 평가: `experiments/nonhuman-followup-v1/HUMAN_EVALUATION.md`
- 임계값 없는 분석: `experiments/nonhuman-followup-v1/human-analysis/THRESHOLD_FREE.md`
- 재현 절차: `experiments/nonhuman-followup-v1/REPRODUCE.md`

## 후속 확장과 제품 적용

2026-09-09 추가14+84개에서는 편집 분할·순서·인물 재현·색/재질·무변경 반복을 비교했다. 그중12개에 사람1명과 별도AI1세션 평가를 연결했다. 무료 FLUX.2 Klein4B에서는 같은 P04의 두 시드·8개 새 출력으로 일괄/순차를 비교했고, 두 시드 모두 고정 얼굴 지표가 일괄에 유리했다. 후속12개 및 무료 모델 실험은 위 초기16개 수치에 합산하지 않는다.

원본 기반 생성 정책을 로컬 편집기에 적용했다. 대화는 현재 요구를 갱신하고 생성은 매번 최초 원본을 참조한다. 플랫폼 기능 검증은 연구 표본과 분리한다. 무료 모델의 결과가 추가됐지만 모델 전반의 우월성을 주장하는 것은 아니다. 세부 수치·평가 범위·최신 검증은 [현재 상태](CURRENT_STATUS.md)에 있다.
