# 현재 프로젝트 상태 — 2026-09-09

최신 결과와 구현 범위의 기준 문서다. 날짜가 붙은 실험 계획·결과·발표 자료는 해당 시점의 기록이다. 원래 생성 모델의 연구와 무료 로컬 모델의 연구를 합산하지 않는다.

## 실험과 평가 범위

| 묶음 | 규모 | 자동 지표 | AI 얼굴 열화 | 사람 평가 |
| --- | --- | --- | --- | --- |
| 기존10단계·정책 비교 | 생성 인물6명, 순차7경로; 최종 고유16개 | 있음 | 단계별 고유111개, 두 세션; 최종16개 포함 | 최종16개, 한 명의 예비 평가 |
| 추가 생성 실험 | 예비14 + 후속84 =98개 고유 출력, 기존 P04/P05/P06 | 전체98개 | 아래 선정12개만 | 아래 선정12개만 |
| 후속 설문 선정 출력 | 위98개 중3인물×2방법×2반복 =12개 | 있음 | 사람 결과를 전달하지 않은 별도 세션1개 | 한 명, 열화12+보존12; 두 열화 답 재평가 반영 |
| 무료 FLUX 실행 확인 | P04, 출력1개 | 기존 연구와 직접 비교하지 않음 | 실행자 관찰만 | 없음 |
| 무료 FLUX 일괄·순차 비교 | P04, 두 시드, 새 출력8개·최종4개 | 고정 얼굴 MAE/SSIM/LPIPS | 실행자 관찰만; 독립 등급 없음 | 없음 |

로컬 편집기의 생성 테스트 이미지는 기능 검증 자료이며 연구 표본에 더하지 않는다. 재평가 두 문항은 새 사람이나 독립 관측 두 개가 아니다. 원본 대 원본 대조와 공유 출력도 본 표본에 더하지 않는다.

## 최신 결과

- 기존 실험: 순차 최종7개를 사람은 모두 심함·미유지, 원본 기반 최종6개는 없음/약함·유지로 평가했다. 한 번의 중간 재생성은 이후 보존을 보장하지 않았다.
- 후속98개: 세 인물에서 일괄의 얼굴 원본 거리가 더 작았고, 무변경 반복도 차이가 누적됐다. 보편적인 최적 순서나 편집 내용의 단순한 강도 관계는 찾지 못했다.
- 후속 사람12개: 일괄 열화 없음2/약함4, 순차 약함4/뚜렷함2. 얼굴 유지 일괄6/6, 순차1/6. 두 문항 재평가를 반영한 수치다.
- 후속 AI12개: 일괄 약함6, 순차 뚜렷함2/심함4. 사람과 등급 정확 일치4/12, 한 등급 이내10/12, 순위 상관0.639. AI가 대체로 더 높은 등급을 주었다.
- 무료 FLUX 두 시드: 둘 다 일괄이 MAE·SSIM·LPIPS에서 유리. 평균 LPIPS 일괄0.091313/순차0.124455, 일괄26.6% 낮음. 경로 전체 시간 평균 약93초/261초. 같은 최종 요구를 주되 호출 수는1회/3회다.

원본 거리 감소율은 사람 피부 열화 감소율이 아니다. 소수 생성 인물과 한 명의 사람 예비 평가로 일반적인 얼굴 보존 성공률·검증된 자동 감지기·최적 임계값을 주장하지 않는다. 새 FLUX 결과는 다른 모델에서의 제한된 재현 사례이며 모든 모델의 우월성 증명이 아니다.

## 서비스 상태

**공개 설문**: 기존32문항, 후속24문항, 특정 두 문항 재평가를 별도 schema로 저장한다. 관리자가 응답·CSV를 확인한다. 이메일 발송은 구현하지 않았다. [운영 안내](evaluation/README.md).

**원본 스튜디오**: localhost:8770의 로컬 전용 편집기. 업로드 → 요구사항 누적 → 설정 확인/수정 → 최초 원본 기반 생성 → 결과/설정 버전 복원. 셔츠 색·배경·원형 핀만 지원한다. 이미지마다 원본에서 생성하고 이전 생성물은 다음 이미지 입력으로 사용하지 않는다. 업로드 원본을 보존하고 추론 입력을 최대512×768로 축소한다.

요청 해석은 기본 표현에 대한 동작 해석 + Qwen3 4B의 구조화된 동작 보조로 구성한다. 모호한 이전 시점/취소 요청은 상태를 바꾸지 않고 버전 선택을 안내한다. 이미지 생성은 FLUX.2 Klein4B 4bit이며 두 모델을 동시에 실행하지 않는다. 모든 문장을 정확히 이해하거나 픽셀 수준으로 얼굴·옷 세부를 보존하는 기능은 아니다. [실행 안내](local-studio/README.md).

## 검증과 기록

- 후속84개: 저장된 입력 연결·출력 해시·지표·피부 영역 재계산 검증. [통합 검산](experiments/autonomous-nonhuman-v1/audit.json).
- 후속 사람/AI: 같은 이미지 해시·얼굴 ROI 연결 및 재평가 반영. [AI 연결 검증](experiments/followup-ai-v1/verification.json).
- 무료 FLUX8개: 해시·최종 프롬프트 일치·원본 대조·고정 ROI 확인. [검증](experiments/local-policy-v1/verification.json).
- 로컬 편집기: 실제 한국어 요청→이미지 생성→설정 변경→복원 검증. 이후 요청 해석 수정은 한국어/영어35개와 연속3요청을 확인했다. 35개는 회귀/개발 검증이며 일반 정확도 추정이 아니다. 임의 배경 표현의 실제 모델 경로도 별도 확인했다. [최신 요청 검증](local-studio/checks/RECHECK.md).
- 초기 한국어10/14, 영어11/14 실패 기록과 초기 구현은 진단 이력으로 보존한다. 현행 품질 수치는 최신 재검증을 참조한다.

기존 PPT는 초기 연구 발표 스냅샷으로, 후속84회·12개 평가·로컬 모델·편집기 내용이 들어 있지 않다. 최신 상태는 이 문서와 연결된 결과를 사용한다. [전체 자료 목록](RESEARCH_INDEX.md).

## 저장소 경계

공개 가능한 연구 코드·계획·지표·이미지·요약과 서비스 소스를 저장한다. 사람 원문/비공개 매핑, 관리자 비밀번호, 로컬 DB/편집기 데이터, 가상환경, 모델 가중치와 실배포 설정은 Git에서 제외한다. 일부 분석은 비공개 원문이나 별도 설치 환경이 필요하며 저장소만 복제해서 모든 생성·분석이 자동 재현되는 것은 아니다.
